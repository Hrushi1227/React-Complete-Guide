import React from "react";

const StepOtp = ({ onNextStep }) => {
  return (
    <>
      <div>StepOtp</div>
      <button onClick={onNextStep}> Next Window </button>
    </>
  );
};

export default StepOtp;
